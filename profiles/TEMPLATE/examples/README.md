# <Project Name> — Examples / Calibration Data

> **NON-CANONICAL. NON-PRODUCTION. NOT EVIDENCE.**
> Nothing in this directory is a rule, a proof, or an authority. It exists
> to calibrate the profile system against real-world shapes.

<!--
Copy this directory when creating a profile from TEMPLATE. Each dataset
gets its own subdirectory with its own README following this shape:

## <dataset-name>/
- **What it is:** <one line — e.g. "synthetic corpus mirroring the shape of
  workstream X">
- **What it calibrates:** <which profile rule, template field, or workflow
  it exercises>
- **Provenance:** <synthetic / reduced-from-development-work / sketched —
  NEVER production data, secrets, or real customer content>
- **Expiry:** <when this example stops being representative — tie it to a
  project version or decision record if possible>

Rules (from profiles/README.md, restated because they matter):
- Synthetic or heavily reduced material only.
- No scope contract may cite examples/** as evidence.
- Lessons learned here get promoted UP into the profile (or core, if truly
  universal); the example itself never becomes law.
-->
